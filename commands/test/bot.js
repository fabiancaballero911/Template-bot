const Discord = require('discord.js');

module.exports = {
    name: "comando",//aca el nombre del comando
    description: "descri",//la descripción del comando

    async run (client, message, args) {
//code qe se ejecutara al poner el comando con su prefix
    }
}
